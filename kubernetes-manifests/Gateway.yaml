apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: backend-graphql-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: TCP
      hosts:
        - "*"  # Replace with your domain or use *