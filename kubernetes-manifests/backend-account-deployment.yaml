apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-account-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-account-deployment
  template:
    metadata:
      labels:
        app: backend-account-deployment
    spec:
      containers:
        - name: backend-account-deployment-container
          image: sujalsharma/backend-account-deployment:v1
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: ecommerce-config
